// Forces the banner to float bottom-center instead of Iubenda's default
// top placement, which was rendering on top of / inside our fixed
// header navigation.
export const IUBENDA_BANNER_POSITION = "float-bottom-center";
